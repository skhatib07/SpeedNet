#include "Layer.h"

Layer::Layer(vector<Neuron*> _neurons){this->neurons = _neurons;}

Layer::Layer(int numOfNodes){neurons = new }

int Layer::ReLU(int num){ return max(0,num);}

int Layer::getActivation(int idx){
    
    for(unsigned int i = 0; i < )
}